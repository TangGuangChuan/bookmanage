
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookmanage?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  mail:
    host: smtp.163.com
    username: 18725969870@163.com
    password: loving19960214
    default-encoding: UTF-8

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.DefaultComponentSafeNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

  thymeleaf:
    cache: false
    check-template-location: true
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
#定时任务配置
schedule:
  enable: true
  cron: 0 0/1 * * * ?

server:
  port: 8080
  servlet:
    context-path: /bookmanage

